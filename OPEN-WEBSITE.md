# 開啟 AZ Productions 網站預覽

1. 下載完整網站 ZIP，解壓後保留 `index.html`、`pui-ching.html`、`site.css`、`site.js` 和 `assets` 資料夾在同一資料夾。
2. 用 Chrome 或 Safari 開啟 `index.html`。首頁的「培正團案例」連結可開啟案例頁。

只下載或移動單一 HTML 檔案會令相片、樣式及互動功能無法載入。GitHub 的檔案預覽頁只顯示程式碼；請使用 **Code → Download ZIP** 下載整個 repository，或使用下方的本機預覽包。

The site is a static website. No build step or local server is required for the downloaded preview.
